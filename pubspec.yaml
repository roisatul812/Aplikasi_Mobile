name: praktikum_mobile_1
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # state management dari modul
  provider: ^6.1.5
  flutter_riverpod: ^2.6.1
  http: ^1.2.2
  dio: ^5.0.0
  shared_preferences: ^2.5.3

  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true